name: gym_app
description: "Gym app"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: "3.6.0"
  flutter: ">=3.27.1"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_test:
      sdk: flutter
  integration_test:
      sdk: flutter

  go_router: 14.6.3
  flutter_bloc: 8.1.6
  get_it: 8.0.3
  drift: 2.23.1


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: 5.0.0

flutter:

  uses-material-design: true
