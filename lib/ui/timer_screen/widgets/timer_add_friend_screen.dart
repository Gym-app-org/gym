import 'package:flutter/material.dart';

class TimerAddFriendScreen extends StatelessWidget {
  const TimerAddFriendScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add friend'),
      ),
    );
  }
}
